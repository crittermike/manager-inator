import React from 'react'
import ReactDOM from 'react-dom/client'
import { TrayCapture } from './components/TrayCapture'
import './styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TrayCapture />
  </React.StrictMode>
)
